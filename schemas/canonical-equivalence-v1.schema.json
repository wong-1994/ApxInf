{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://apxinf.dev/schemas/canonical-equivalence-v1.schema.json",
  "title": "ApxInf private Canonical VLA equivalence evidence",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "port_id",
    "mode",
    "contract",
    "source_inventory_sha256",
    "canonical_trace_sha256",
    "thresholds",
    "parameter_mapping",
    "source_parameters",
    "canonical_parameters",
    "source_aliases",
    "canonical_aliases",
    "source_tied_weights",
    "canonical_tied_weights",
    "transformations",
    "cases",
    "summary"
  ],
  "properties": {
    "schema_version": {"const": "1.0"},
    "port_id": {"type": "string", "minLength": 1},
    "mode": {"enum": ["direct", "canonicalized"]},
    "contract": {"$ref": "#/$defs/contract"},
    "source_inventory_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "canonical_trace_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "thresholds": {
      "type": "object",
      "additionalProperties": false,
      "required": ["absolute", "relative"],
      "properties": {
        "absolute": {"type": "number", "minimum": 0},
        "relative": {"type": "number", "minimum": 0}
      }
    },
    "parameter_mapping": {
      "type": "array",
      "items": {"$ref": "#/$defs/parameterMapping"}
    },
    "source_parameters": {
      "type": "array",
      "items": {"$ref": "#/$defs/tensor"}
    },
    "canonical_parameters": {
      "type": "array",
      "items": {"$ref": "#/$defs/tensor"}
    },
    "source_aliases": {"$ref": "#/$defs/nameGroups"},
    "canonical_aliases": {"$ref": "#/$defs/nameGroups"},
    "source_tied_weights": {"$ref": "#/$defs/nameGroups"},
    "canonical_tied_weights": {"$ref": "#/$defs/nameGroups"},
    "transformations": {
      "type": "array",
      "items": {"$ref": "#/$defs/transformation"}
    },
    "semantic_rewrites": {"type": "array"},
    "state_semantics": {"type": "array"},
    "branches": {"type": "array"},
    "intermediate_mapping": {"type": "array"},
    "preprocessing_mapping": {"type": "array", "minItems": 1},
    "cases": {
      "type": "array",
      "minItems": 4,
      "items": {"$ref": "#/$defs/case"}
    },
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["cases", "comparisons", "failures"],
      "properties": {
        "cases": {"type": "integer", "minimum": 4},
        "comparisons": {"type": "integer", "minimum": 1},
        "failures": {"type": "integer", "minimum": 0}
      }
    }
  },
  "allOf": [
    {
      "if": {"properties": {"mode": {"const": "canonicalized"}}},
      "then": {
        "required": [
          "semantic_rewrites",
          "state_semantics",
          "branches",
          "intermediate_mapping",
          "preprocessing_mapping"
        ]
      }
    }
  ],
  "$defs": {
    "contract": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "sha256"],
      "properties": {
        "version": {"type": "string", "pattern": "^[0-9]+\\.[0-9]+$"},
        "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
      }
    },
    "stringArray": {
      "type": "array",
      "items": {"type": "string", "minLength": 1}
    },
    "parameterMapping": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sources", "targets", "transformation_ids"],
      "properties": {
        "sources": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "targets": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "transformation_ids": {"$ref": "#/$defs/stringArray"}
      }
    },
    "tensor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "shape", "dtype", "requires_grad"],
      "properties": {
        "name": {"type": "string", "minLength": 1},
        "shape": {
          "type": "array",
          "items": {"type": "integer", "minimum": 0}
        },
        "dtype": {"type": "string", "minLength": 1},
        "requires_grad": {"type": "boolean"}
      }
    },
    "nameGroups": {
      "type": "array",
      "items": {
        "type": "array",
        "minItems": 2,
        "items": {"type": "string", "minLength": 1}
      }
    },
    "transformation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "category",
        "rewrite",
        "assumptions",
        "source_paths",
        "target_paths",
        "description"
      ],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "category": {
          "enum": [
            "transpose",
            "split",
            "concatenation",
            "packing",
            "mask",
            "conditioning",
            "cache",
            "schedule"
          ]
        },
        "rewrite": {"enum": ["algebraic", "numerical_equivalence"]},
        "assumptions": {"$ref": "#/$defs/stringArray"},
        "source_paths": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "target_paths": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "description": {"type": "string", "minLength": 1}
      }
    },
    "comparison": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "scope",
        "source_path",
        "canonical_path",
        "passed",
        "max_absolute_error",
        "max_relative_error",
        "mismatch_reason"
      ],
      "properties": {
        "scope": {
          "enum": ["preprocessed_inputs", "intermediates", "normalized_actions", "postprocessed_actions"]
        },
        "source_path": {"type": "string", "minLength": 1},
        "canonical_path": {"type": "string", "minLength": 1},
        "passed": {"type": "boolean"},
        "max_absolute_error": {"type": ["number", "null"], "minimum": 0},
        "max_relative_error": {"type": ["number", "null"], "minimum": 0},
        "mismatch_reason": {"type": ["string", "null"]}
      }
    },
    "case": {
      "type": "object",
      "additionalProperties": false,
      "required": ["profile", "seed", "comparisons", "passed"],
      "properties": {
        "profile": {"type": ["string", "null"]},
        "seed": {"enum": [0, 1]},
        "comparisons": {
          "type": "array",
          "minItems": 4,
          "items": {"$ref": "#/$defs/comparison"}
        },
        "passed": {"type": "boolean"}
      }
    }
  }
}
